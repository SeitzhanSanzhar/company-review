import React, { ReactElement } from 'react'

interface Props {
    
}

export default function InterviewAdd({}: Props): ReactElement {
    return (
        <div>
            <h1>add</h1>
        </div>
    )
}
